{
  "name": "@typeparameter/todo-mcp",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "^24"
  },
  "scripts": {
    "build": "turbo build",
    "deploy": "pnpm run --filter ./apps/infra deploy",
    "deploy:hotswap": "pnpm run --filter ./apps/infra deploy:hotswap",
    "dev": "turbo dev",
    "format": "prettier --cache --write .",
    "format:check": "prettier --cache --check .",
    "lint": "turbo lint"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "@types/node": "^24.12.4",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "turbo": "^2.9.12",
    "typescript": "~5.9.3"
  }
}
