{
  "name": "@typeparameter/todo-mcp-server",
  "private": true,
  "type": "module",
  "exports": {
    "./handler": "./build/handler"
  },
  "scripts": {
    "build": "node esbuild.ts",
    "dev": "node --env-file=.env bin/http-server.ts",
    "lint": "tsc && eslint --cache --max-warnings=0 .",
    "lint:fix": "tsc && eslint --cache --max-warnings=0 --fix ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@typeparameter/todo-mcp-data-access": "workspace:*",
    "@typeparameter/todo-mcp-models": "workspace:*",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "@typeparameter/todo-mcp-conventions": "workspace:*",
    "@types/aws-lambda": "^8.10.161",
    "esbuild": "^0.27.4"
  }
}
